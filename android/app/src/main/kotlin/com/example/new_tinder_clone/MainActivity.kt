package com.example.new_tinder_clone

import io.flutter.embedding.android.FlutterActivity

class MainActivity: FlutterActivity() {
    // The flutter_facebook_auth plugin will handle Facebook SDK initialization
    // No need to override onCreate or initialize Facebook SDK manually
}